<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
    <% end %>

    <% if flash[:users] %>
    <div id='notice'><%= flash[:users] %></div>
    <% end %>

    <%= yield %>  <!-- this then yields to the view which has been called by your controller -->
  </body>
</html>